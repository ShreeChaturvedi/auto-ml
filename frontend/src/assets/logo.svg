<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- AutoML Logo - Neural Network "A" -->
  <!-- An abstract A formed by connected nodes representing ML/AI -->

  <!-- Top apex node -->
  <circle cx="16" cy="4" r="3" fill="currentColor"/>

  <!-- Bottom left node -->
  <circle cx="5" cy="28" r="3" fill="currentColor"/>

  <!-- Bottom right node -->
  <circle cx="27" cy="28" r="3" fill="currentColor"/>

  <!-- Center crossbar node -->
  <circle cx="16" cy="17" r="2.5" fill="currentColor"/>

  <!-- Left leg connection -->
  <path d="M13.5 6L7 26" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>

  <!-- Right leg connection -->
  <path d="M18.5 6L25 26" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>

  <!-- Crossbar left segment -->
  <path d="M9.5 17H13.5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>

  <!-- Crossbar right segment -->
  <path d="M18.5 17H22.5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
</svg>
