import type { TimelineFixture } from "../types";

/**
 * Beat 1 — Landing Scroll.
 *
 * TODO: Populate with scrollTo + zoom + sfx events keyed to the
 * alignment marks in voiceover/scripts/scene-landing.txt:
 *   {{HERO}} {{PREVIEW}} {{PHASES}} {{CHAT}} {{META}} {{CLOSER}} {{HANDOFF}}
 *
 * See plan §1.3 (scroll mechanics) and §1.6 (zoom moments).
 */
export const LANDING_SCROLL: TimelineFixture = {
  id: "landing-scroll",
  events: [],
} as const;
